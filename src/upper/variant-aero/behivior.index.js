import IDENTIFIER from './behavior.@fn._.IDENTIFIER';
import QUERY from "./behavior.@fn._.QUERY";
import RECORD from './behavior.@fn._.RECORD';
import DIALOG from './behavior.@fn._.DIALOG';

export default {
    IDENTIFIER,
    QUERY,
    RECORD,
    DIALOG,
}