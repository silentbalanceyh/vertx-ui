import React from 'react';

/*
 * 字典管理
 * Tabular专用模块
 * 1）链接地址和 React-Router实现绑定
 */
class Component extends React.PureComponent {
    render() {
        return (
            <div>
                Component
            </div>
        );
    }
}

export default Component;