import Ux from "ux";

const connectOp = (reference, from, to) => {
    if (Ux.Env.NODE_ENV === "development") {

    }
};
const connectForm = (reference, data) => {
    if (Ux.Env.NODE_ENV === "development") {

    }
};
export default {
    connectOp,
    connectForm
}