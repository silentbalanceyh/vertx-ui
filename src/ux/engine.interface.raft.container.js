import __Zs from 'zs';

const raftContainer = (cell = {}, config = {}) =>
    __Zs.raftContainer(cell, config);

// eslint-disable-next-line import/no-anonymous-default-export
export default {
    raftContainer,
}