import renderCmd from './Web.Fn.Cmd';
import renderPalette from './Web.Fn.Palette';
import renderStatus from './Web.Fn.Status';
import renderDrawer from './Web.Fn.Drawer';

export default {
    renderCmd,
    renderPalette,
    renderStatus,
    renderDrawer,
}