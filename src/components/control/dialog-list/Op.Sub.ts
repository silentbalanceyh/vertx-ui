const $opSubSave = (reference: any) => (values) => {
    console.info(values);
};
const $opSubAdd = (reference: any) => (values) => {
    console.info(values);
};

export default {
    $opSubSave,
    $opSubAdd
}