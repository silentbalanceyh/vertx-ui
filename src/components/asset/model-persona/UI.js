import React from 'react';

class Component extends React.PureComponent {
    render() {
        return (
            <div>
                数据资产 / 画像建模 / 画像配置
            </div>
        )
    }
}

export default Component